@model LoginModel

@{
    ViewBag.Title = "Administrative console login";
}

<ifs-page-title>@ViewBag.Title</ifs-page-title>

<p>
    Please enter your credentials to continue.
</p>

@if (!ViewData.ModelState.IsValid) {
    <div asp-validation-summary="All" class="alert alert-danger"></div>
}

<form asp-action="Login" asp-antiforgery="true">
    <input type="hidden" asp-for="ReturnUrl"/>

    <div class="form-floating mb-3">
        <input asp-for="UserName" placeholder-for="UserName" class="form-control" />
        <label asp-for="UserName"></label>
    </div>

    <div class="form-floating mb-3">
        <input asp-for="Password" placeholder-for="UserName" class="form-control"/>
        <label asp-for="Password"></label>
    </div>

    <div class="mb-3">
        <button class="btn btn-primary">Continue</button>
    </div>
</form>