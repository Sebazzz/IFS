@model FileIdentifier
@{
    Layout = "_Frame";
}

<script>
    (function (doc) {
        const targetUrl = '@Url.Action("Complete", new { id = Model })';

        doc.uploadCoordinator.complete(targetUrl);
    })(window.parent);
</script>